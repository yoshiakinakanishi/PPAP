import * as React from 'react';
import Layout from '../layouts';
import Promotion from '../components/promotion';

import { Link } from '../routes';

export default () => (
    <Layout title="プロモーション一覧">
        <Promotion/>
    </Layout>
);